name: Trigger Netlify Build
on:
  schedule:
    - cron: '30 * * * *' # at minute 30
jobs:
  build:
    name: Request Netlify Webhook
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -X POST -d {} https://api.netlify.com/build_hooks/5e81a224b8d12b79191c9268
